var checking = require('./check');

checking.username = "sbanda";
checking.password = "123456";

checking.check('test.com', function (err, available) {
  if(err) return console.log(err);
  console.log("google.com -->" , available);
})